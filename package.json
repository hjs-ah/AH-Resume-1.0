{
  "name": "ah-portfolio",
  "version": "2.0.0",
  "description": "Antone Holmes — Portfolio with Notion CMS",
  "scripts": {
    "build": "node build-data.js",
    "dev": "node build-data.js && python3 -m http.server 8000"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15"
  },
  "engines": {
    "node": ">=18.x"
  }
}
